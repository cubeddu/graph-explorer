import * as React from "react";
import {
  Button,
  Header,
  Icon,
  SpaceBetween,
} from "@cloudscape-design/components";
export default function LayoutHeader() {
  const [visible, setVisible] = React.useState(false); // replace with jotai state to trigger new connections modal globally
  return (
    
  );
}
